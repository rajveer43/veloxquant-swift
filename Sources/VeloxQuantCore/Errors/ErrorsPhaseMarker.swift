/// `VeloxQuantError` — the full `Error`, `LocalizedError`, `Sendable` error hierarchy, all
/// eleven cases defined together in Phase 1 even though some (AutoPilot, process-lifecycle)
/// aren't reachable until later phases.
enum ErrorsPhaseMarker {}
